@model IEnumerable<fbpm_cs.Models.PaymentSchedule>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Schedule ID
        </th>
        <th>
            Schedule Date
        </th>
        <th>
            Schedule Amount
        </th>
        <th>
            Schedule Percent
        </th>
        <th>
            Remaining Amount
        </th>
        <th></th>
    </tr>

@{
    try
    {
        if(Model.Count() > 0){
        foreach (var item in Model)
        {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduleID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduleDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduleAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SchedulePercent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RemainingAmount)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
        }
      }
    }
    catch (System.NullReferenceException e)
    {
        Console.WriteLine("Error reading from {0}. Message = {1}", e.Message);
    
     }
}

</table>
